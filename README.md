# Firebase Auth App

This is a sample Firebase Authentication application using React and TypeScript.

## Installation
1. Clone the repository.
2. Install dependencies using `npm install`.

## Usage
1. Create a `.env` file.
2. Run the app using `npm start`. 
